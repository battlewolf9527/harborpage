# HarborPage - 前端构建时环境变量示例（Vite）
# 将此文件复制为 .env.local 并填写前端配置
# 
# 注意：
# - 此文件中的变量会被打包进前端代码，不要放敏感信息！
# - Worker 运行时变量请使用 .dev.vars 文件
# - 环境变量需要 VITE_ 前缀才能在前端代码中访问

# ==================== 前端配置（如需） ====================
# VITE_APP_TITLE=HarborPage
# VITE_DEBUG=false
