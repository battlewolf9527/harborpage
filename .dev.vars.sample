# HarborPage - Worker 环境变量示例
# 将此文件复制为 .dev.vars 并填写实际的 API Key
# 
# 说明：
# - .dev.vars 由 Wrangler CLI 在本地开发时加载
# - 变量会注入到 Worker 运行时，通过 env.xxx 访问
# - 生产环境请使用 wrangler secret put 命令设置敏感变量

# ==================== 认证配置 ====================
PASSWORD=<YOUR_LOGIN_PASSWORD>
JWT_SECRET=<YOUR_JWT_SECRET>

# ==================== 天气 API 配置 ====================
# 天气功能开关 + 供应商（可选：qweather、openmeteo）；不填写则不启用天气功能
# WEATHER_PROVIDER=qweather
# 和风天气（qweather）：在 https://dev.qweather.com/ 注册获取免费 API Key
WEATHER_API_KEY=<YOUR_WEATHER_API_KEY>
WEATHER_API_HOST=<YOUR_WEATHER_API_HOST>
# Open-Meteo（openmeteo）：无需密钥，设置 WEATHER_PROVIDER=openmeteo 即可启用
